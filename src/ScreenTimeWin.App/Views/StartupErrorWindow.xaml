<Window x:Class="ScreenTimeWin.App.Views.StartupErrorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:p="clr-namespace:ScreenTimeWin.App.Properties"
        mc:Ignorable="d"
        Title="{x:Static p:Resources.StartupErrorTitle}" Height="400" Width="600"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
            <TextBlock Text="❌" FontSize="32" VerticalAlignment="Center" Margin="0,0,15,0"/>
            <StackPanel VerticalAlignment="Center">
                <TextBlock Text="{x:Static p:Resources.AppFailedToStart}" FontSize="20" FontWeight="Bold"/>
                <TextBlock Text="{x:Static p:Resources.CriticalErrorInit}" FontSize="14" Foreground="Gray"/>
            </StackPanel>
        </StackPanel>

        <TextBox Grid.Row="1" x:Name="ErrorTextBox" 
                 IsReadOnly="True" VerticalScrollBarVisibility="Auto" 
                 TextWrapping="Wrap" FontFamily="Consolas" FontSize="12"
                 Background="#F5F5F5" Padding="10" Margin="0,0,0,15"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="{x:Static p:Resources.OpenLogs}" Width="120" Height="30" Margin="0,0,10,0" Click="OpenLogs_Click"/>
            <Button Content="{x:Static p:Resources.CopyError}" Width="100" Height="30" Margin="0,0,10,0" Click="CopyError_Click"/>
            <Button Content="{x:Static p:Resources.Exit}" Width="80" Height="30" Click="Exit_Click"/>
        </StackPanel>
    </Grid>
</Window>
