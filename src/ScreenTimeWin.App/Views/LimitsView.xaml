<UserControl x:Class="ScreenTimeWin.App.Views.LimitsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:ScreenTimeWin.App.ViewModels"
             xmlns:p="clr-namespace:ScreenTimeWin.App.Properties"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900"
             Background="{DynamicResource WindowBackgroundBrush}">
    
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>  <!-- 页面标题 -->
            <RowDefinition Height="Auto"/>  <!-- 使用摘要卡片 -->
            <RowDefinition Height="*"/>     <!-- 限制列表 -->
        </Grid.RowDefinitions>

        <!-- 1. 页面标题 -->
        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel Orientation="Horizontal">
                <Border Width="32" Height="32" CornerRadius="8" Background="#F9AB00" Margin="0,0,12,0">
                    <TextBlock Text="⏱️" FontSize="16" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
                <TextBlock Text="{x:Static p:Resources.UsageLimitsTitle}" FontSize="22" FontWeight="SemiBold" VerticalAlignment="Center"
                           Foreground="{DynamicResource PrimaryTextBrush}"/>
            </StackPanel>
            
            <Button Grid.Column="2" Command="{Binding NavigateToFocusCommand}" 
                    Content="{x:Static p:Resources.FocusModeTitle}"
                    Background="Transparent" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1"
                    Padding="12,6" Foreground="{DynamicResource PrimaryTextBrush}" Cursor="Hand">
                    <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Padding="{TemplateBinding Padding}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="🎯" Margin="0,0,6,0"/>
                                <ContentPresenter/>
                            </StackPanel>
                        </Border>
                            <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource HoverBackgroundBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </Grid>

        <!-- 2. 使用摘要卡片 -->
        <Border Grid.Row="1" Background="{DynamicResource CardBackgroundBrush}" CornerRadius="12" Padding="24" Margin="0,0,0,20">
            <Border.Effect>
                <DropShadowEffect BlurRadius="20" ShadowDepth="2" Opacity="0.06" Color="#000000"/>
            </Border.Effect>
            
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <!-- 今日使用 -->
                <StackPanel Grid.Column="0" HorizontalAlignment="Center">
                    <TextBlock Text="{x:Static p:Resources.Today}" FontSize="14" Foreground="{DynamicResource SecondaryTextBrush}" 
                               HorizontalAlignment="Center"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,4,0,0">
                        <TextBlock Text="{Binding TodayUsedText}" FontSize="28" FontWeight="Bold" 
                                   Foreground="{DynamicResource PrimaryTextBrush}"/>
                        <TextBlock Text="{x:Static p:Resources.Used}" FontSize="14" Foreground="{DynamicResource SecondaryTextBrush}" 
                                   VerticalAlignment="Bottom" Margin="4,0,0,5"/>
                    </StackPanel>
                </StackPanel>
                
                <!-- 分隔符 -->
                <Border Grid.Column="1" Width="1" Background="{DynamicResource DividerBrush}" Margin="30,0"/>
                
                <!-- 剩余时间 -->
                <StackPanel Grid.Column="2" HorizontalAlignment="Center">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,4">
                        <TextBlock Text="{Binding RemainingText}" FontSize="28" FontWeight="Bold" 
                                   Foreground="{DynamicResource SuccessBrush}"/>
                        <TextBlock Text="{x:Static p:Resources.Remaining}" FontSize="14" Foreground="{DynamicResource SecondaryTextBrush}" 
                                   VerticalAlignment="Bottom" Margin="4,0,0,5"/>
                    </StackPanel>
                    <!-- 紧迫限制提示 -->
                    <Border Background="#FEF3E2" CornerRadius="4" Padding="10,4" HorizontalAlignment="Center">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="⚠️" FontSize="12" Margin="0,0,6,0"/>
                            <TextBlock Text="{Binding UrgentLimitText}" FontSize="12" Foreground="#B45309"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Grid>
        </Border>

            <!-- 3. Limit List Area -->
            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="24"/>
                    <ColumnDefinition Width="350"/>
                </Grid.ColumnDefinitions>

                <!-- Left: Limits List -->
                <Border Grid.Column="0" Style="{DynamicResource CardStyle}" Padding="0,16">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        
                        <!-- Header -->
                        <Grid Margin="24,0,24,16">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <TextBlock Text="{x:Static p:Resources.LimitList}" FontSize="18" FontWeight="SemiBold" Foreground="{DynamicResource PrimaryTextBrush}"/>
                            
                            <!-- Filter -->
                            <StackPanel Grid.Column="1" Orientation="Horizontal">
                                <Border Background="{DynamicResource TabActiveBackgroundBrush}" CornerRadius="12" Padding="12,6" Margin="0,0,8,0" Cursor="Hand">
                                    <TextBlock Text="{x:Static p:Resources.Active}" FontSize="13" FontWeight="SemiBold" Foreground="{DynamicResource TabActiveTextBrush}"/>
                                </Border>
                            </StackPanel>
                        </Grid>
                        
                        <!-- List -->
                        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="12,0">
                            <ItemsControl ItemsSource="{Binding CategoryLimits}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Background="{DynamicResource WindowBackgroundBrush}" CornerRadius="12" Padding="16" Margin="0,6">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                
                                                <!-- Icon -->
                                                <Border Width="40" Height="40" CornerRadius="10" Background="{DynamicResource CardBackgroundBrush}" Margin="0,0,16,0">
                                                    <TextBlock Text="{Binding CategoryIcon}" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </Border>
                                                
                                                <!-- Text -->
                                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                                    <TextBlock Text="{Binding Name}" FontSize="15" FontWeight="Medium" Foreground="{DynamicResource PrimaryTextBrush}"/>
                                                    <TextBlock Text="{Binding LimitText}" FontSize="13" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,2,0,0"/>
                                                </StackPanel>
                                                
                                                <!-- Warning -->
                                                <Border Grid.Column="2" Background="#FEF3E2" CornerRadius="6" Padding="8,4" Margin="0,0,16,0" VerticalAlignment="Center"
                                                        Visibility="{Binding IsWarning, Converter={StaticResource BooleanToVisibilityConverter}}">
                                                    <StackPanel Orientation="Horizontal">
                                                        <TextBlock Text="⚠️" FontSize="12" Margin="0,0,4,0"/>
                                                        <TextBlock Text="{x:Static p:Resources.NearLimit}" FontSize="12" FontWeight="Medium" Foreground="#B45309"/>
                                                    </StackPanel>
                                                </Border>
                                                
                                                <!-- Toggle -->
                                                <ToggleButton Grid.Column="3" IsChecked="{Binding IsActive}" Width="48" Height="28" VerticalAlignment="Center">
                                                    <ToggleButton.Template>
                                                        <ControlTemplate TargetType="ToggleButton">
                                                            <Border x:Name="border" Background="#E8EAED" CornerRadius="14" Width="48" Height="28">
                                                                <Ellipse x:Name="thumb" Width="24" Height="24" Fill="White" HorizontalAlignment="Left" Margin="2,0,0,0">
                                                                    <Ellipse.Effect>
                                                                        <DropShadowEffect BlurRadius="4" ShadowDepth="1" Opacity="0.2"/>
                                                                    </Ellipse.Effect>
                                                                </Ellipse>
                                                            </Border>
                                                            <ControlTemplate.Triggers>
                                                                <Trigger Property="IsChecked" Value="True">
                                                                    <Setter TargetName="border" Property="Background" Value="{DynamicResource AccentBrush}"/>
                                                                    <Setter TargetName="thumb" Property="HorizontalAlignment" Value="Right"/>
                                                                    <Setter TargetName="thumb" Property="Margin" Value="0,0,2,0"/>
                                                                </Trigger>
                                                            </ControlTemplate.Triggers>
                                                        </ControlTemplate>
                                                    </ToggleButton.Template>
                                                </ToggleButton>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                    </Grid>
                </Border>

                <!-- Right: Quick Actions -->
                <Border Grid.Column="2" Style="{DynamicResource CardStyle}">
                    <StackPanel>
                        <TextBlock Text="{x:Static p:Resources.QuickActions}" FontSize="16" FontWeight="SemiBold" Foreground="{DynamicResource PrimaryTextBrush}" Margin="0,0,0,20"/>
                        
                        <!-- Add Limit Button -->
                        <Button Command="{Binding AddLimitCommand}" Background="{DynamicResource AccentBrush}" Foreground="White" Padding="0,14" Cursor="Hand" BorderThickness="0">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" CornerRadius="12">
                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                                            <TextBlock Text="+" FontSize="18" FontWeight="Bold" Margin="0,0,8,1" Foreground="White"/>
                                            <TextBlock Text="{x:Static p:Resources.AddLimit}" FontSize="15" FontWeight="SemiBold" Foreground="White"/>
                                        </StackPanel>
                                    </Border>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                        
                        <Border Height="1" Background="{DynamicResource DividerBrush}" Margin="0,24"/>
                        
                        <!-- Tips -->
                         <TextBlock Text="{x:Static p:Resources.Tips}" FontSize="14" FontWeight="SemiBold" Foreground="{DynamicResource PrimaryTextBrush}" Margin="0,0,0,12"/>
                         <StackPanel>
                             <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                 <TextBlock Text="•" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,0,8,0"/>
                                 <TextBlock Text="{x:Static p:Resources.LimitTip1}" FontSize="13" Foreground="{DynamicResource SecondaryTextBrush}" TextWrapping="Wrap"/>
                             </StackPanel>
                             <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                 <TextBlock Text="•" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,0,8,0"/>
                                 <TextBlock Text="{x:Static p:Resources.LimitTip2}" FontSize="13" Foreground="{DynamicResource SecondaryTextBrush}" TextWrapping="Wrap"/>
                             </StackPanel>
                             <StackPanel Orientation="Horizontal">
                                 <TextBlock Text="•" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,0,8,0"/>
                                 <TextBlock Text="{x:Static p:Resources.LimitTip3}" FontSize="13" Foreground="{DynamicResource SecondaryTextBrush}" TextWrapping="Wrap"/>
                             </StackPanel>
                         </StackPanel>
                    </StackPanel>
                </Border>
            </Grid>
    </Grid>
</UserControl>
