<UserControl x:Class="ScreenTimeWin.App.Views.LimitsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:ScreenTimeWin.App.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900"
             Background="{DynamicResource WindowBackgroundBrush}">
    
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>  <!-- 页面标题 -->
            <RowDefinition Height="Auto"/>  <!-- 使用摘要卡片 -->
            <RowDefinition Height="*"/>     <!-- 限制列表 -->
        </Grid.RowDefinitions>

        <!-- 1. 页面标题 -->
        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <StackPanel Orientation="Horizontal">
                <Border Width="32" Height="32" CornerRadius="8" Background="#F9AB00" Margin="0,0,12,0">
                    <TextBlock Text="⏱️" FontSize="16" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
                <TextBlock Text="⏱️ 使用限制" FontSize="22" FontWeight="SemiBold" VerticalAlignment="Center"
                           Foreground="{DynamicResource PrimaryTextBrush}"/>
            </StackPanel>
        </Grid>

        <!-- 2. 使用摘要卡片 -->
        <Border Grid.Row="1" Background="{DynamicResource CardBackgroundBrush}" CornerRadius="12" Padding="24" Margin="0,0,0,20">
            <Border.Effect>
                <DropShadowEffect BlurRadius="20" ShadowDepth="2" Opacity="0.06" Color="#000000"/>
            </Border.Effect>
            
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <!-- 今日使用 -->
                <StackPanel Grid.Column="0" HorizontalAlignment="Center">
                    <TextBlock Text="今日:" FontSize="14" Foreground="{DynamicResource SecondaryTextBrush}" 
                               HorizontalAlignment="Center"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,4,0,0">
                        <TextBlock Text="{Binding TodayUsedText}" FontSize="28" FontWeight="Bold" 
                                   Foreground="{DynamicResource PrimaryTextBrush}"/>
                        <TextBlock Text=" 已用" FontSize="14" Foreground="{DynamicResource SecondaryTextBrush}" 
                                   VerticalAlignment="Bottom" Margin="4,0,0,5"/>
                    </StackPanel>
                </StackPanel>
                
                <!-- 分隔符 -->
                <Border Grid.Column="1" Width="1" Background="{DynamicResource DividerBrush}" Margin="30,0"/>
                
                <!-- 剩余时间 -->
                <StackPanel Grid.Column="2" HorizontalAlignment="Center">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,4">
                        <TextBlock Text="{Binding RemainingText}" FontSize="28" FontWeight="Bold" 
                                   Foreground="{DynamicResource SuccessBrush}"/>
                        <TextBlock Text=" 剩余" FontSize="14" Foreground="{DynamicResource SecondaryTextBrush}" 
                                   VerticalAlignment="Bottom" Margin="4,0,0,5"/>
                    </StackPanel>
                    <!-- 紧迫限制提示 -->
                    <Border Background="#FEF3E2" CornerRadius="4" Padding="10,4" HorizontalAlignment="Center">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="⚠️" FontSize="12" Margin="0,0,6,0"/>
                            <TextBlock Text="{Binding UrgentLimitText}" FontSize="12" Foreground="#B45309"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Grid>
        </Border>

        <!-- 3. 限制列表区域 -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="300"/>
            </Grid.ColumnDefinitions>

            <!-- 左侧：应用限制列表 -->
            <Border Grid.Column="0" Background="{DynamicResource CardBackgroundBrush}" CornerRadius="12" Padding="0,16">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="20" ShadowDepth="2" Opacity="0.06" Color="#000000"/>
                </Border.Effect>
                
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <!-- 标题和筛选 -->
                    <Grid Margin="20,0,20,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        
                        <TextBlock Text="限制列表" FontSize="16" FontWeight="SemiBold" 
                                   Foreground="{DynamicResource PrimaryTextBrush}"/>
                        
                        <!-- Active/Inactive 筛选 -->
                        <StackPanel Grid.Column="1" Orientation="Horizontal">
                            <Border Background="{DynamicResource TabActiveBackgroundBrush}" CornerRadius="4" 
                                    Padding="12,4" Margin="0,0,8,0" Cursor="Hand">
                                <TextBlock Text="已启用" FontSize="12" FontWeight="Medium" 
                                           Foreground="{DynamicResource TabActiveTextBrush}"/>
                            </Border>
                            <Border Background="Transparent" CornerRadius="4" 
                                    Padding="12,4" Cursor="Hand">
                                <TextBlock Text="▾" FontSize="12" Foreground="{DynamicResource SecondaryTextBrush}"/>
                            </Border>
                        </StackPanel>
                    </Grid>
                    
                    <!-- 规则列表 -->
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                        <ItemsControl ItemsSource="{Binding CategoryLimits}" Margin="12,0">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="{DynamicResource WindowBackgroundBrush}" CornerRadius="10" 
                                            Padding="16,14" Margin="0,4" Cursor="Hand">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            
                                            <!-- 分类图标 -->
                                            <Border Width="36" Height="36" CornerRadius="8" 
                                                    Background="{DynamicResource CardBackgroundBrush}" Margin="0,0,14,0">
                                                <TextBlock Text="{Binding CategoryIcon}" FontSize="16" 
                                                           HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                            
                                            <!-- 名称和限制 -->
                                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                                <TextBlock Text="{Binding Name}" FontSize="14" FontWeight="Medium"
                                                           Foreground="{DynamicResource PrimaryTextBrush}"/>
                                                <TextBlock Text="{Binding LimitText}" FontSize="12" 
                                                           Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,2,0,0"/>
                                            </StackPanel>
                                            
                                            <!-- 警告标签 -->
                                            <Border Grid.Column="2" Background="#FEF3E2" CornerRadius="4" 
                                                    Padding="8,3" Margin="0,0,12,0" VerticalAlignment="Center"
                                                    Visibility="{Binding IsWarning, Converter={StaticResource BooleanToVisibilityConverter}}">
                                                <TextBlock Text="快超" FontSize="11" FontWeight="Medium" Foreground="#B45309"/>
                                            </Border>
                                            
                                            <!-- 开关 -->
                                            <ToggleButton Grid.Column="3" IsChecked="{Binding IsActive}" Width="44" Height="24" VerticalAlignment="Center">
                                                <ToggleButton.Template>
                                                    <ControlTemplate TargetType="ToggleButton">
                                                        <Border x:Name="border" Background="#E8EAED" CornerRadius="12" Width="44" Height="24">
                                                            <Ellipse x:Name="thumb" Width="20" Height="20" Fill="White" 
                                                                     HorizontalAlignment="Left" Margin="2,0,0,0">
                                                                <Ellipse.Effect>
                                                                    <DropShadowEffect BlurRadius="4" ShadowDepth="1" Opacity="0.3"/>
                                                                </Ellipse.Effect>
                                                            </Ellipse>
                                                        </Border>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsChecked" Value="True">
                                                                <Setter TargetName="border" Property="Background" Value="#1967D2"/>
                                                                <Setter TargetName="thumb" Property="HorizontalAlignment" Value="Right"/>
                                                                <Setter TargetName="thumb" Property="Margin" Value="0,0,2,0"/>
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </ToggleButton.Template>
                                            </ToggleButton>
                                            
                                            <!-- 展开箭头 -->
                                            <TextBlock Grid.Column="3" Text="›" FontSize="16" 
                                                       Foreground="{DynamicResource SecondaryTextBrush}" 
                                                       VerticalAlignment="Center" Margin="12,0,0,0" Visibility="Collapsed"/>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Grid>
            </Border>

            <!-- 右侧：添加限制面板 -->
            <Border Grid.Column="2" Background="{DynamicResource CardBackgroundBrush}" CornerRadius="12" Padding="20">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="20" ShadowDepth="2" Opacity="0.06" Color="#000000"/>
                </Border.Effect>
                
                <StackPanel>
                    <TextBlock Text="快捷操作" FontSize="15" FontWeight="SemiBold" 
                               Foreground="{DynamicResource PrimaryTextBrush}" Margin="0,0,0,16"/>
                    
                    <!-- 添加限制按钮 -->
                    <Button Command="{Binding AddLimitCommand}" Background="{DynamicResource AccentBrush}" 
                            Foreground="White" Padding="16,12" HorizontalAlignment="Stretch">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                        <TextBlock Text="+" FontSize="16" FontWeight="Bold" Margin="0,0,8,0"/>
                                        <TextBlock Text="添加限制" FontSize="14" FontWeight="Medium"/>
                                    </StackPanel>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                    
                    <!-- 分隔线 -->
                    <Border Height="1" Background="{DynamicResource DividerBrush}" Margin="0,20"/>
                    
                    <!-- 快捷提示 -->
                    <TextBlock Text="提示" FontSize="13" FontWeight="Medium" 
                               Foreground="{DynamicResource PrimaryTextBrush}" Margin="0,0,0,10"/>
                    <TextBlock TextWrapping="Wrap" FontSize="12" Foreground="{DynamicResource SecondaryTextBrush}" LineHeight="20">
                        <Run>• 设置每日限额可以帮助您控制应用使用时间</Run>
                        <LineBreak/>
                        <Run>• 开启宵禁模式可在特定时段阻止应用启动</Run>
                        <LineBreak/>
                        <Run>• 分类限制可以一次性管理多个相似应用</Run>
                    </TextBlock>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
