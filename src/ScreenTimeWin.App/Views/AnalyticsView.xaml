<UserControl x:Class="ScreenTimeWin.App.Views.AnalyticsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
             xmlns:vm="clr-namespace:ScreenTimeWin.App.ViewModels"
             xmlns:p="clr-namespace:ScreenTimeWin.App.Properties"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="{x:Static p:Resources.StatisticsTitle}" FontSize="24" FontWeight="SemiBold" Margin="0,0,0,20"/>

        <!-- 日期选择器 -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,20">
            <DatePicker SelectedDate="{Binding SelectedDate}" Width="150" />
            <Button Content="{x:Static p:Resources.Refresh}" 
                    Command="{Binding LoadDataCommand}" 
                    Margin="10,0,0,0" 
                    Padding="15,8"
                    Background="#4285F4"
                    Foreground="White"
                    BorderThickness="0"
                    Cursor="Hand">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Background" Value="#4285F4"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#3367D6"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#2850A7"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </StackPanel>

        <!-- 图表 -->
        <Grid Grid.Row="2" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="1*" />
            </Grid.ColumnDefinitions>

            <!-- 每小时活动柱状图 -->
            <Border Background="{DynamicResource CardBackgroundBrush}" CornerRadius="8" Padding="15" Margin="0,0,10,0">
                <Grid>
                    <TextBlock Text="{x:Static p:Resources.HourlyActivity}" FontWeight="SemiBold" VerticalAlignment="Top"/>
                    <lvc:CartesianChart Series="{Binding HourlySeries}" XAxes="{Binding HourlyXAxes}" Margin="0,25,0,0"/>
                </Grid>
            </Border>

            <!-- 分类饼图 -->
            <Border Grid.Column="1" Background="{DynamicResource CardBackgroundBrush}" CornerRadius="8" Padding="15" Margin="10,0,0,0">
                <Grid>
                    <TextBlock Text="{x:Static p:Resources.CategoryStats}" FontWeight="SemiBold" VerticalAlignment="Top"/>
                    <lvc:PieChart Series="{Binding CategorySeries}" Margin="0,25,0,0" LegendPosition="Bottom"/>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>

