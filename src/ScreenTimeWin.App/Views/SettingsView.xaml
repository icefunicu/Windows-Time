<UserControl x:Class="ScreenTimeWin.App.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:ScreenTimeWin.App.ViewModels"
             xmlns:p="clr-namespace:ScreenTimeWin.App.Properties"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="{x:Static p:Resources.Settings}" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>

        <ScrollViewer Grid.Row="1">
            <StackPanel>
                <!-- Actions -->
                <Border Background="{DynamicResource CardBackgroundBrush}" CornerRadius="8" Padding="20" Margin="0,0,0,15">
                    <StackPanel>
                        <TextBlock Text="Actions" FontWeight="SemiBold" Margin="0,0,0,10"/>
                        
                        <Button Content="{x:Static p:Resources.ClearData}" Command="{Binding ClearDataCommand}" Background="#D32F2F" HorizontalAlignment="Left" Width="150"/>
                        <Button Content="{x:Static p:Resources.ExportData}" Command="{Binding ExportDataCommand}" HorizontalAlignment="Left" Width="150"/>
                    </StackPanel>
                </Border>

                <!-- Preferences -->
                <Border Background="{DynamicResource CardBackgroundBrush}" CornerRadius="8" Padding="20">
                    <StackPanel>
                        <TextBlock Text="Preferences" FontWeight="SemiBold" Margin="0,0,0,10"/>
                        
                        <Button Content="{x:Static p:Resources.ToggleTheme}" Command="{Binding ToggleThemeCommand}" Margin="0,15,0,0" HorizontalAlignment="Left" Width="180"/>

                        <CheckBox Content="Enable Mock Data Mode (Restart Required)" IsChecked="{Binding IsMockModeEnabled}" Margin="0,15,0,0"/>
                        <TextBlock Text="Use this mode to test UI without running the background service." Foreground="{DynamicResource SecondaryTextBrush}" FontSize="11" Margin="24,2,0,0"/>
                        
                        <CheckBox Content="Start with Windows" IsChecked="{Binding IsStartWithWindowsEnabled}" Margin="0,10,0,0"/>
                        <TextBlock Text="Automatically start the UI when you log in." Foreground="{DynamicResource SecondaryTextBrush}" FontSize="11" Margin="24,2,0,0"/>

                        <Button Content="{x:Static p:Resources.ChangeSecurityPin}" Command="{Binding SetPinCommand}" Margin="0,15,0,0" HorizontalAlignment="Left" Width="150"/>
                        
                        <StackPanel Visibility="{Binding IsPinChangeVisible, Converter={StaticResource BooleanToVisibilityConverter}}" Margin="20,10,0,0" Background="{DynamicResource CardBackgroundBrush}">
                             <TextBlock Text="Old PIN (Leave empty if none):"/>
                             <TextBox Text="{Binding OldPin}" Width="150" HorizontalAlignment="Left" Margin="0,5"/>
                             <TextBlock Text="New PIN (Leave empty to remove):"/>
                             <TextBox Text="{Binding NewPin}" Width="150" HorizontalAlignment="Left" Margin="0,5"/>
                             <Button Content="Save PIN" Command="{Binding ConfirmSetPinCommand}" Width="100" HorizontalAlignment="Left" Margin="0,10,0,0"/>
                        </StackPanel>

                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
