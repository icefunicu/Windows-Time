已修复 UI 无法点击及功能异常的问题。

### 修复内容
1.  **启用模拟数据模式 (Mock Mode)**：默认开启了 `MockAppService`，现在无需运行后台服务即可在 Dashboard 看到演示数据（总时长、Top 应用列表）。
2.  **解锁导航菜单**：移除了侧边栏按钮的 `IsEnabled="False"` 限制，现在可以点击 "Analytics", "Limits", "Focus", "Settings"。
3.  **实现页面导航**：
    - 创建了 `Analytics`, `Limits`, `Focus`, `Settings` 的 ViewModel 和占位视图 (`PlaceholderView`)。
    - 修复了 `MainWindow` 的路由逻辑，现在点击左侧按钮可以正常切换右侧内容。
4.  **修复构建**：重新编译并通过了所有项目的构建。

### 验证方法
请重新运行 UI 项目：
```powershell
cd src/ScreenTimeWin.App
dotnet run
```
现在你应该能看到：
- Dashboard 显示 "Total Screen Time" 和应用列表。
- 左侧导航栏所有按钮均可点击。
- 点击非 Dashboard 页面会显示 "Coming Soon / Under Construction"。
